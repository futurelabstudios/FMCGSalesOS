{
  "name": "fmcgsalesos",
  "version": "1.0.0",
  "description": "Full-stack FMCG Sales OS with AI copilot",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "typecheck": "npm run typecheck --prefix server && npm run build --prefix client -- --mode production",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
